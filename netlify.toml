[build]
  command = "bun run browsers:install && bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  PLAYWRIGHT_BROWSERS_PATH = ".playwright"

[dev]
  command = "bun run dev"

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
